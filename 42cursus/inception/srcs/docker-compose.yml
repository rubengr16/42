version: "3.8"

services:
  nginx-container:
    build:
      context: ./requirements/nginx
    hostname: nginx-container
    ports:
      - 443:443
    networks:
      - net
networks:
  net:
    name: net
    driver: bridge

