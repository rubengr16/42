version: "3.7"
services:
  nginx-container:
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    hostname: nginx-container
    ports:
      - 4242:4242
    networks:
      - net
networks:
  net:
    name: net
    driver: bridge

